context("input/output")

library(ATAComb)


test_that(
  "read_10x_fragments() reads fragments.tsv and generates a list of data.frames, one per cell",
  {

  }
)

test_that(
  "convert_fragments_gr() needs tests",
  {

  }
)
